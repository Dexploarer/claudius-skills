# GitHub Integration (Optional)
# Enable automatic GitHub issue creation for competition results
# Create a Personal Access Token at: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos only)

GITHUB_TOKEN=your_github_personal_access_token_here
GITHUB_OWNER=your-github-username-or-org
GITHUB_REPO=your-repository-name

# GitHub Issue Configuration
# Automatically create issues for findings above this severity
# Options: critical, high, medium, low
GITHUB_MIN_SEVERITY=high

# Label configuration (comma-separated)
GITHUB_LABELS=bug-hunting,security,automated

# Assignee configuration (optional, comma-separated GitHub usernames)
# GITHUB_ASSIGNEES=username1,username2

# Milestone (optional, milestone number)
# GITHUB_MILESTONE=1

# Issue creation mode
# Options: auto (create issues automatically), manual (require explicit command)
GITHUB_ISSUE_MODE=manual
