{
  "mcpServers": {
    "example-server": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-example"],
      "env": {
        "API_KEY": "${API_KEY}",
        "COMMENT": "Use environment variables for secrets"
      },
      "disabled": false,
      "description": "Example MCP server configuration"
    }
  },
  "_comment": "IMPORTANT: Never commit this file with real credentials!",
  "_setup": {
    "1_addToGitignore": "echo '.mcp.json' >> .gitignore",
    "2_setEnvVar": "export API_KEY='your-api-key-here'",
    "3_testConnection": "Start Claude and verify server connects"
  }
}
